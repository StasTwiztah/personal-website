import { createTheme } from "@mui/material";

export const lightTheme = createTheme({
  palette: {
    primary: {
      main: "#272643",
      dark: "#2A2F3F",
      light: "#383E52",
    },
    background: {
      default: "#fff",
      paper: "#f5f5f5",
    },
  },
});
